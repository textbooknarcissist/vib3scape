{
  // Root config — uses project references. Paths alias (@/) is declared in tsconfig.app.json.
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ]
}
